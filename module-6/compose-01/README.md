# Setup

To start all the services in detached mode:
```bash
docker-compose up -d
```

If you just want to build all the images rather than running the container you can do:
```bash
docker-compose build
```

To stop all services:
```bash
docker-compose down
```
