{
  "name": "test",
  "version": "1.0.0",
  "description": "If you want to run a command, or view something that requires a specific environment to run you can do this using Docker.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^5.2.1"
  }
}
